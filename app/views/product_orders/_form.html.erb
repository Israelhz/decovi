<%= form_for(product_order) do |f| %>
  <% if product_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_order.errors.count, "error") %> prohibited this product_order from being saved:</h2>

      <ul>
      <% product_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.select :producto_id, options_for_select(@productos.collect { |producto|
    [producto.nombre + ' ' + producto.acabado, producto.id] }, 1) %>


  <div class="field">
    <%= f.label :cantidad %>
    <%= f.number_field :cantidad %>
  </div>

  <div class="actions">
    <%= f.hidden_field :pedido_id, value: @pedido.id %>
    <%= f.submit %>
  </div>
<% end %>
