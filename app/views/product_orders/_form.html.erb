<%= form_for(product_order) do |f| %>
  <% if product_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_order.errors.count, "error") %> prohibited this product_order from being saved:</h2>

      <ul>
      <% product_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <% @productos.each do |producto| %>
      <div class="col-md-2 text-center">
        <%= image_tag producto.image.url, :class => "thumb-image" %>
        <%= f.radio_button :producto_id, producto.id, :required => true %>
        <%= f.label :producto_id, producto.nombre %>
      </div>
    <% end %>


  </div>
  
  <div class="field">
    <%= f.label :cantidad %>
    <%= f.number_field :cantidad, :required => true, :min => 1%>
  </div>
  
  <div class="divide40"></div>
  <div class="actions">
    <%= f.hidden_field :pedido_id, value: @pedido.id %>
    <%= f.submit :value => 'Añadir producto', :class => 'btn btn-primary' %>
  </div>
<% end %>
